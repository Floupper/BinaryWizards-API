<<<<<<< HEAD
image: node:latest

test:
  script:
    - npm install
    - npm test --coverage --coverageDirectory=coverage --coverageProvider=babel --outputFile=test-results.xml
  artifacts:
    when: always
    reports:
      junit: coverage/test-results.xml
    paths:
      - coverage/test-results.xml
  allow_failure: true

build:
  needs:
    - test
  script:
    - echo "Build process started..."
    - echo "Build process completed."
  artifacts:
    when: on_success

deploy:
  needs:
    - build
  script:
    - echo "Deploy process started..."
    - echo "Deploy process completed."
=======
stages:
  - trigger_clients_pipelines

trigger_web:
  stage: trigger_clients_pipelines
  trigger:
    project: "binarywizards/s5-binarywizards-web"
    branch: "devops"  
    strategy: depend

trigger_mobile:
  stage: trigger_clients_pipelines
  trigger:
    project: "binarywizards/s5-binarywizards-mobile"
    branch: "devops"
    strategy: depend
>>>>>>> 1e1043751e999d9beafda0909f8e2f0de80e6c0c
